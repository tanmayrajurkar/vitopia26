NEXT_PUBLIC_API_URL = "https://vitopia-backend.vercel.app/api"

# Supabase (for HLS hero video: bucket "video", segment_011–035; segment_001–010 from repo)
NEXT_PUBLIC_SUPABASE_URL = "https://your-project-ref.supabase.co"
# Hybrid HLS: first 10 segments from public/hls/, rest from Supabase (faster start)
# NEXT_PUBLIC_USE_HYBRID_HLS = true
# Optional: full manifest URL if not using hybrid
# NEXT_PUBLIC_HLS_VIDEO_URL =

# Instant hero load (100–150ms): single MP4 URL. Prefer over HLS when set.
# Export with faststart: ffmpeg -i input.mp4 -c copy -movflags +faststart hero.mp4
# NEXT_PUBLIC_HERO_VIDEO_MP4_URL = "https://your-project.supabase.co/storage/v1/object/public/video/hero.mp4"
AUTH_SECRET= "" # Generate a secret using `openssl rand -base64 32`
NEXTAUTH_URL= "http://localhost:3000" # The base URL of the site (e.g. http://localhost:3000)
AUTH_GOOGLE_ID= "" # Google Client ID
AUTH_GOOGLE_SECRET= "" # Google Client Secret

NEXTJS_ENV=development

DATABASE_URL="postgresql://username:password@host:port/db"

NEXT_API_TOKEN = ""

JWT_SECRET = ""


# AWS S3 Bucket
AWS_KEY_ID= "" # AWS Access Key ID
AWS_SECRET_ACCESS_KEY= "" # AWS Secret Access Key
AWS_REGION= "" # AWS Region
AWS_BUCKET_NAME= "" # AWS S3 Bucket Name